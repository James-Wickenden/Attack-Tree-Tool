"use strict";

function instantiateTree(goal) {
    const atree = {};
    atree.nodes = [];

}

function addNode() {
    const node = {};
    node.goal = "Something";
    node.attributes = [];
    node.parent = -1;
}